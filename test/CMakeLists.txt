# Tests are intended to be run under PlatformIO/Arduino (Unity).
# For plain CMake builds, we skip building tests.

message(STATUS "ESPTimer: tests are managed via PlatformIO; skipping CMake test builds.")

